<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AllActivity.devloper" />
        <activity android:name=".AllActivity.feedback" />
        <activity android:name=".AllActivity.complain" />
        <activity android:name=".AllActivity.eventy" />
        <activity android:name=".AllActivity.exam" />
        <activity android:name=".AllActivity.faculty" />
        <activity android:name=".AllActivity.profile" />
        <activity android:name=".AllActivity.dashbord" />
        <activity android:name=".AllActivity.screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AllActivity.singin" />
        <activity android:name=".AllActivity.MainActivity" />

    </application>

</manifest>